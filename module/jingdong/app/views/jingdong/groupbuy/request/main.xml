<?xml version="1.0" encoding="utf-8"?>
<Request xmlns="http://tuan.360buy.com/Response">
    *{ 版本 }*
    <Version>${version}</Version>
    *{ 合作伙伴ID }*
    <VenderId>${venderId}</VenderId>
    <!--合作伙伴Key-->
    <VenderKey>${venderKey}</VenderKey>
    *{ 是否压缩 默认不压缩 }*
    <Zip>${zip}</Zip>
    *{ 是否加密 }*
    <Encrypt>${encrypt}</Encrypt>
    *{ 提交数据 }*
    #{if encrypt == 'true'}
        <Data>${models.jingdong.JDGroupBuyUtil.encryptMessage(data)}</Data>
    #{/if}
    #{else}
        <Data>${data}</Data>
    #{/else}
</Request>