self: play -> supplier-navigation 0.2

require:
    - play 1.2.4
    - base_rbac 0.1

repositories:
    - custom0Modules:
        type:       local
        artifact:   "${application.path}/../../module/[module]"
        contains:
            - base_rbac -> *
