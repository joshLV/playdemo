# Application dependencies

require:
    - play
    - play -> cas 3.1
    - sales -> sales
    - consumer -> consumer
    - order -> order 
    - com.uhuila.common - file 1.0.0
    - web_trace -> web_trace
    - accounts  -> accounts

repositories:
    - customModules:
        type:       local
        root:       "http://dev.uhuila.cn/nexus/content/groups/public"
        artifact:   "${application.path}/../../module/[module]"
        contains:
            - sales -> *
            - consumer -> *
            - order -> *
            - com.uhuila.common -> *
            - web_trace -> *
            - accounts  -> *
