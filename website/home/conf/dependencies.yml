# Application dependencies

require:
    - play
    - sales -> sales
    - order -> order
    - consumer -> consumer
     - play -> cas 3.1
    

repositories:
    - customModules:
        type:       local
        artifact:   "${application.path}/../../module/[module]"
        contains:
            - sales -> *
            - order -> *
            - consumer -> *